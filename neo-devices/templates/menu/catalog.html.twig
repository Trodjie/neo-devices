<ul class="nav nav-pills flex-column mb-auto">
    {% for item in items %}
        <li class="nav-item">
            <a href="{{ item.path }}" class="nav-link" aria-current="page">
                {{ item.title }}
            </a>
        </li>
    {% endfor %}
    <li>
</ul>
